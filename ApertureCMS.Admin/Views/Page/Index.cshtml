@model IEnumerable<ApertureCMS.Models.Page>

@{
    ViewBag.Title = "Index";
}
<div class="row">
     <div class="span12">
        <h2 class="page-title">Pages <small>All Pages</small></h2>
    </div>           
</div>   
 @foreach (var item in Model)
    {
<div class="row">   
        <div class="span12">
            <section class="widget">
                <header>
                    <h4>
                        <i class="fa fa-align-left"></i>
                        @item.Title @Html.ActionLink("Edit", "Edit", new {id = item.PageId})
                       @Html.ActionLink("Delete", "Delete", new { id = item.PageId })


                    </h4>

                </header>
                <div class="body">
                    @Html.Raw(item.Content)
                </div>
                </section>
        </div>   
</div>
 }
<div class="row">
    <div class="span12">
        <section class="widget">
            <header>
                <h4>
                    <i class="fa fa-plus-sign"></i>
                     @Html.ActionLink("New Page", "Create")
                </h4>

            </header>
            <div class="body">
                Create a new content page
            </div>
        </section>
    </div>
</div>